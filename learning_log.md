# 无人车项目学习日志

## 2026-08-25｜实践开发第 1 天

今天是两周项目进入实践开发后的第一天。我在昨天理论学习的基础上，开始进行 STM32 车灯功能改进和 OpenHarmony 开发环境配置，并结合实际操作学习了 Linux、SSH 与 Git 的基本使用方法。

STM32 开发方面，我先阅读了 `main.c`、`usart.c`、`colorful_led.c` 和相关头文件，重点梳理了串口接收、状态判断和 WS2812 灯光输出之间的关系。原程序通过 USART1 接收指令，在接收状态满足条件后进入灯效函数，但原有灯效进入循环后缺少明确的停止控制。针对这个问题，我将串口控制扩展为启动和停止两种状态：电脑发送 `HELLO` 后，小车启动前后车灯效果；电脑发送 `STOP!` 后，程序将前灯颜色设置为熄灭状态，同时调用后灯关闭函数，并将串口状态恢复为等待状态。为了丰富车灯表现，我还调整了前灯的彩色循环过程，并补充了后灯的联动显示。最新版程序已经通过 Keil 编译并烧录到小车，`HELLO` 和 `STOP!` 均已完成实车验证，灯光启动、色彩变化和关闭效果符合预期。

本次 STM32 验证形成了较为完整的操作闭环。Keil 编译结果为 ' 0 Error' '0 warning'，ST-Link 完成程序下载后，小车能够正常启动。电脑通过 CH340 对应的 COM5 与 STM32 通信，串口参数设置为 115200 波特率、8 位数据位、1 位停止位、无校验和无流控。STM32 的输出信息经过 USART1 的 PA9 引脚发送到串口助手，电脑发送的控制命令则经过 PA10 进入串口接收中断。中断程序识别命令并修改接收状态，主循环再根据状态调用灯光函数。这一过程验证了从电脑命令、串口传输、程序判断到车灯响应的完整信息流。

OpenHarmony 环境配置方面，我完成了虚拟机启动和网络配置，虚拟机目前能够正常访问网络。我随后安装并配置了 VS Code 开发插件，通过 Remote-SSH 建立了 VS Code 与 Harmony 虚拟机之间的远程连接。连接成功后，我在远程环境中打开开发目录，并完成了指定 Python 文件的编译运行。本次操作让我实际理解了宿主机、虚拟机、SSH 服务和远程开发工具之间的关系，也掌握了通过 VS Code 直接编辑和执行虚拟机内文件的基本流程。本阶段暂未进行 OpenHarmony 或 Hi3861 工程的完整编译测试。

代码管理方面，我使用 PowerShell 同步脚本将开发目录中的 STM32 和 Hi3861 源代码单向同步到 GitHub 展示目录，并通过预览模式检查了新增、修改和删除项目。同步过程中，我练习了 `git status`、`git diff`、`git add`、`git commit` 等基本命令，理解了工作区、暂存区、本地仓库和远程仓库之间的区别。我还发现 μVision 保存后的源码版本晚于展示目录中的版本，因此重新执行同步并检查文件差异，避免了旧版 `main.c` 被提交到展示仓库。

环境配置和代码管理过程中出现的问题也帮助我建立了基本的排查思路。串口助手最初选择了不存在的 COM1，我通过设备管理器确认 CH340 实际对应 COM5，并区分了负责烧录的 ST-Link 与负责串口通信的 CH340。ST-Link 安装驱动后从其他设备移动到通用串行总线设备，设备名称和黄色感叹号的变化说明驱动已经生效。代码同步阶段出现的版本滞后并不是 Git 覆盖了源码，而是展示目录保存的是上一次同步结果；我通过文件修改时间、大小和哈希值确认差异，重新同步后再检查暂存内容，完成了版本修正。

借助agent工具,我将当天使用的 Linux 和 Git 基础知识整理为操作笔记。Linux 部分包括目录和文件操作、虚拟机网络检查、SSH 连接关系以及远程环境中的 Python 文件执行；Git 部分包括工作区、暂存区、本地仓库和远程仓库的作用，以及 `status`、`diff`、`add`、`commit` 和 `push` 的基本流程。笔记内容均与当天的虚拟机配置、远程连接和代码同步操作对应，后续可以继续补充 OpenHarmony 编译和双系统联调过程中使用的新命令。

今天已经完成 STM32 车灯功能改进及实车验证、Harmony 虚拟机联网、VS Code Remote-SSH 连接、指定 Python 文件运行和项目代码同步。下一阶段将继续熟悉 Linux 开发环境，并逐步开展 OpenHarmony、Hi3861 与 STM32 双系统联动相关的开发和验证。

## 2026-08-26｜实践开发第 2 天

今天的实践从 OpenHarmony 多线程运行逐步推进到舵机和车轮控制，使我开始把任务调度、互斥访问和 PWM 等概念与小车的实际动作联系起来。上午首先完成了 Hi3861 上的多线程输出程序：两个同优先级线程分别按照 1 秒和 3 秒的周期打印信息。程序经过工程配置、编译、HiBurn 烧录和串口验证后能够稳定运行。串口中交替出现的输出让我直观看到，线程延时并不是让整个系统停住，而是当前线程进入阻塞状态，由系统调度其他可以运行的线程。

在此基础上，我继续使用多个线程控制 SG90 舵机。舵机由 GPIO2 输出周期为 20 ms 的脉冲，不同的高电平持续时间对应不同角度；程序运行后能够观察到舵机转向 90° 和 180°，串口也能显示三个线程的运行信息。多个线程可能同时操作同一根舵机控制线，如果各自输出的高低电平脉冲相互穿插，最终形成的波形将不再代表有效角度。因此，互斥锁在这里并不是单纯为了满足程序结构，而是为了保证一段完整的舵机控制过程只能由一个线程执行。实际输出中可以观察到线程获取、占用和释放互斥锁的先后关系，同时未使用该互斥锁的线程仍能继续运行。通过这一现象，我理解了互斥锁保护的是共享资源，而不是让所有任务停止；等待锁的线程会暂时阻塞，锁释放后才重新获得执行机会。

下午的 STM32 开发重点是利用 TIM4 的 PWM 控制左右车轮。定时器设置为 `PWM_Init(7199, 9)` 后，在 72 MHz 时钟下形成约 1 kHz 的 PWM，比较寄存器中的数值决定输出占空比。最初使用的速度值 `3000` 约占计数周期的 42%，因此车轮能够稳定转动但速度偏慢；进一步测试表明，提高比较值会增加平均驱动电压和车轮转速，但数值不能无限增加，因为当前自动重装载值决定了有效范围为 `0～7199`。这使我明确了代码中的 PWM 数值代表占空比控制量，而不是电机的实际转速；真实速度还会受到电池电压、负载、机械阻力和电机差异影响。

完成左右轮对应关系验证后，我将底层 `Set_Pwm()` 封装成更容易理解的整车运动接口，并实际验证了前进、后退、弧线转弯和原地旋转。弧线转弯通过左右轮同向但不同速实现，车辆在前进过程中改变轨迹；原地旋转则让左右轮等速反向，旋转中心接近车体中央。两种动作虽然都能改变车头方向，但设计目的不同：前者用于正常行驶转向，后者用于狭窄空间调整朝向。测试过程中还出现过函数重复定义的链接错误，检查后发现电机函数同时写入了 `main.c` 和 `motor.c`。修正文件分工后，我进一步理解了头文件负责接口声明、源文件负责函数实现、主程序负责调用的组织方式。

今天完成的内容形成了两条相互关联的认识：在 Hi3861 上，多个任务要通过调度和互斥机制安全地共享硬件；在 STM32 上，定时器产生的 PWM 则把程序中的控制量转化为电机速度和运动轨迹。任务五、任务七和任务二十一均已完成编译、烧录与运行验证，舵机角度、线程输出、左右轮动作以及多种运动形式均能按程序表现。相比只让示例代码运行起来，今天更重要的收获是能够结合实际现象解释互斥锁为什么必要、PWM参数如何影响执行器，以及不同运动方式背后的差速控制关系。

## 2026-08-27｜实践开发第 3 天

今天完成了任务八、任务二十二和任务二十三。与前两天相比，今天不再只是让单个外设产生动作，而是开始围绕“测量—反馈—修正”解决真实车辆运行中的问题：Hi3861通过超声波获取外部距离，STM32通过编码器测量车轮状态，再根据测量结果动态调整电机输出。三个任务表面上分别属于传感器、定时器和控制算法，实际共同构成了无人车感知与运动控制的基础。

任务八首先完成了HC-SR04超声波测距程序。程序由GPIO7向Trig端输出20 μs触发脉冲，GPIO8读取Echo回响信号，`hi_get_us()`记录高电平持续时间，最后按照声波往返距离公式换算为厘米。实际开发中遇到的主要问题并不在测距公式，而在工程组织和编译环节：创建OpenHarmony线程时，`Hcsr04()`先使用了后面才定义的`Hcsrtext`，编译器无法识别该任务函数；同时子工程与APP目录中的`BUILD.gn`必须同时配置正确，源码才能被纳入构建。通过查看构建日志、补充`Hcsrtext`函数声明并逐项核对目标名、源码名和工程路径，最终完成了工程编译和测距程序运行。这个过程让我认识到，Ninja最后显示的“return code is 1”只是失败结果，真正有效的排查入口是构建日志中最先出现的具体错误。

随后进行的任务二十二为闭环控制建立了可靠的反馈基础。刚开始阅读编码器程序时，代码中的A、B变量、TIM2、TIM3以及左右轮名称并不完全一致，部分旧注释还把TIM3误写成TIM4、把GPIOA误写成GPIOB。如果只根据变量名称判断，很容易把左右轮关系接反。因此我没有套用常见小车的方向经验，而是分别转动车轮，通过串口观察有符号计数，最终实车确认`Read_Encoder(2)`读取左轮、`Read_Encoder(3)`读取右轮，并确认两个编码器的正负号均正确。系统每100 ms读取一次计数并清零，连续输出的脉冲数能够反映车轮在相同采样周期内的实际速度变化。通过这一步，我掌握的不只是定时器编码器模式的配置，更重要的是学会了用独立单轮实验和串口数据验证软件变量与真实硬件之间的对应关系。

任务二十三的调试最能体现今天解决问题的过程。闭环程序最初运行时，小车只能向前移动很短一段，随后开始旋转。起初这个现象容易被理解为左右目标速度或PID参数不合适，但沿着数据流逐层检查后发现，根本原因是反馈串轮：TIM2左轮反馈计算得到`Motor_A`，却通过`Set_Pwm()`第1参数驱动了右轮；TIM3右轮反馈计算得到`Motor_B`，却通过第2参数驱动了左轮。这样一个车轮的误差会被用来修正另一个车轮，控制器越调节，左右输出反而越容易分离。确认底层第1参数对应右轮PWMB/BIN、第2参数对应左轮PWMA/AIN后，我只交换了一处调用顺序，将输出改为`Set_Pwm(Motor_B, Motor_A)`，使左右编码器分别控制同侧电机，消除了反馈串轮。

闭环对应关系修正后，小车已经能够持续前进，但实车轨迹仍有轻微左偏。此时我没有再次改动底层方向或同时修改多个控制参数，而是保持左轮目标`1.0`，逐步降低右轮目标，最终使用`0.97`作为当前车辆的前进修正值。后退调试则单独进行，因为同一辆车前进和后退时受到的机械阻力、轮胎接触状态和电机响应并不完全相同，最终后退阶段使用左右轮`-1.0`的目标组合。为了形成可重复验证的实验过程，我增加了运动周期计数：车辆先前进约3秒，再后退约3秒，最后停止；串口同时持续输出编码器值、目标值、PWM值和固件版本号，使每次修改、编译和烧录都能通过实时数据确认，而不是仅凭肉眼判断车辆动作。

在观察反馈变化的过程中，我进一步核对了控制器的实际公式。虽然函数名称保留为`Incremental_PI`，代码也声明了Kp、Ki和Kd，但当前真正参与PWM计算的是`Pwm += Kp × Error + Kd × (Error - Error_prev)`，Ki和已经累计的积分值并没有进入最终输出。因此，当前程序确实会根据编码器误差实时增减PWM，但它并不是完整的标准PID。这个发现改变了我的调参思路：车辆持续偏向一侧时，应先调整左右目标比例；响应过快、振荡或输出饱和时，再调整Kp和Kd；当前直接修改Ki并不会产生预期效果。相比机械地尝试三个参数，这种先确认公式、再根据现象选择参数的方法更有依据。

今天最大的收获不是简单完成了三个实验，而是建立了一套更可靠的软硬件联合调试方法。面对编译失败，我学会从第一条有效错误定位声明和工程配置问题；面对编码器映射混乱，我通过单轮实验和串口数据确认真实连接；面对车辆旋转和跑偏，我先梳理“目标值—编码器—控制器—PWM通道—实际车轮”的完整链路，再坚持一次只修改一个因素并进行实车复测。最终，小车完成了编码器同侧闭环控制以及前进、后退和停车流程，超声波模块也实现了距离测量。更重要的是，我开始能够解释车辆为什么会出现某种现象，以及代码中的哪一层应该为这个现象负责，这为后续避障、循迹和多传感器协同控制打下了基础。

## 2026-08-28｜实践开发第 4 天

今天完成了任务十一“OpenHarmony系统驱动实验——OLED显示字符串”。在前几天已经能够使用GPIO、定时器和串口控制外设的基础上，今天开始接触I2C总线及SSD1306显示驱动。任务最终不仅实现了静态字符串显示，还完成了预设文本、日期和动态时间的组合显示。时间能够每秒持续刷新，并在秒、分、小时达到上限后正常进位，OLED整体显示稳定，最终效果与课程示例一致。

本次开发没有出现明显的功能性技术故障，主要工作集中在理解并完成SSD1306驱动的工程集成。开始编写应用前，我先对照课程说明和小车原理图梳理OLED的连接关系。OLED通过SCL和SDA两根信号线接入Hi3861的I2C总线，SCL负责同步通信时序，SDA负责双向传输数据。与普通GPIO直接输出高低电平不同，I2C通信还需要区分主从设备、设备地址和ACK应答；Hi3861作为主机，通过设备地址选择SSD1306，再把控制命令和显示数据写入OLED。通过这一过程，我对两线总线上挂接外设的基本通信方式形成了更具体的认识。

工程集成是今天最需要认真核对的部分。我将SSD1306驱动的头文件、字模文件和源文件组织到应用工程中，并修改子工程的`BUILD.gn`：在`sources`中加入应用程序和`hal_bsp_ssd1306.c`，在`include_dirs`中加入驱动头文件目录以及I2C、CMSIS-RTOS2所需的系统路径；随后在APP目录的`BUILD.gn`中启用OLED子模块。通过这一步，我进一步明确了源文件存在于目录中并不等于会自动参与编译，只有构建目标、源码列表和头文件搜索路径同时正确，应用代码才能找到并链接外设驱动。

程序运行时先调用SSD1306初始化接口建立I2C通信并配置显示屏，再清除原有显示内容，随后写入预设标题和日期。动态时间部分使用时、分、秒变量保存状态，每隔一秒更新一次；当秒数超过59时将秒清零并增加分钟，分钟超过59后增加小时，小时超过23后重新从0开始。每次刷新前先清空字符串缓冲区，再通过`sprintf()`把数值格式化成两位的时、分、秒字符串，最后调用字符串显示接口写入指定位置。实际显示证明初始化、字符串格式化、时间进位和周期刷新能够连续配合工作。

在阅读驱动代码时，我还梳理了应用层、设备驱动层和底层I2C接口之间的调用关系。应用程序负责决定显示内容和刷新周期，SSD1306驱动负责把字符和字模转换为显示控制命令与显存数据，底层则通过`I2cInit()`设置总线频率，并使用`I2cWrite()`完成实际数据发送；`I2cRead()`和`I2cSetBaudrate()`分别提供读取设备数据和调整通信速率的能力。字符显示看起来只是调用一个函数，实际背后包含字符串处理、字模查找、显示坐标计算和I2C数据传输，这使我对外设API封装的作用有了更清楚的理解。

今天的理论收获主要是理解了I2C总线的通信机制、SCL与SDA的作用、设备寻址和应答过程，也了解了SSD1306字符显示本质上是把字模数据写入对应显存位置。实践方面，我完成了SSD1306驱动文件的组织、`BUILD.gn`配置、应用层接口调用以及动态时间显示，对OpenHarmony模块的构建流程和外设驱动接入方式更加熟悉。相比单纯复制示例代码，本次任务更重要的收获是能够沿着“应用逻辑—驱动接口—I2C底层通信—OLED硬件显示”的完整链路解释程序如何工作，为后续在OLED上显示超声波距离、轮速、传感器数据和系统状态打下了基础。

随后完成了任务十二“OpenHarmony系统驱动实验——SHT20温湿度采集”。本任务继续使用I2C总线，但实验重点从向OLED写入显示数据转向读取传感器测量结果，并通过计数信号量协调三个同优先级线程。程序中线程1每个周期释放两次信号量，线程2获得一次信号量后调用SHT20驱动读取温度和湿度，线程3获得另一次信号量后输出同步信息。程序完成编译、烧录和运行验证后，串口能够稳定显示温度、湿度以及三个线程释放和获得信号量的信息，说明SHT20数据采集与多线程同步均能正常工作。

工程第一次编译时，GN在生成构建文件阶段提示无法加载`8.0_Sht20/BUILD.gn`。开始时目录和APP总构建项看起来都已存在，但进一步对照报错路径和实际文件名后发现，子工程文件被命名为`Build.gn`。Windows环境通常不容易暴露这种大小写差异，而Linux严格区分文件名大小写，因此GN无法把它识别为规定名称的`BUILD.gn`。将文件名改为完全一致的大写形式后，构建系统成功生成目标并进入C源码编译。这次问题让我认识到，排查“Unable to load”不能只确认目录是否存在，还要逐字符核对路径、文件名大小写和构建目标名称。

进入源码编译后又出现`thread1`、`thread2`和`thread3`未声明的错误。结合错误行号检查程序结构后发现，`i2c_sht20_demo()`在创建线程时已经使用了这三个任务函数，但它们的函数定义位于文件后部，编译器在当前调用位置还不知道这些标识符。通过在任务创建函数之前补充三个函数声明，使编译器提前获得函数名称和类型信息，错误得到解决。这个问题进一步说明，Ninja末尾的`subcommand failed`只代表某个编译步骤失败，真正的修改依据仍然是前面包含源码路径、行号和错误标识符的第一组信息。

通过观察串口输出，我对计数信号量的工作方式有了直观认识。信号量不是简单通知某一个固定线程，而是保存可用资源数量；线程1连续释放两次后，等待同一信号量的线程2和线程3分别获得一次执行机会。线程2完成SHT20读取并输出实际温湿度，线程3输出获得信号量的信息，随后两个线程再次进入等待状态，直到线程1进入下一个释放周期。与任务七使用互斥锁保护单一硬件资源相比，互斥锁强调同一时刻只能有一个线程占用资源，而本任务中的计数信号量更适合进行任务通知和执行次数协调。

任务十二使我在任务十一I2C输出实验的基础上完成了I2C传感器输入验证，并进一步熟悉了驱动文件、头文件、应用代码和`BUILD.gn`之间的组织关系。更重要的是，本次两次构建失败形成了清晰的排查过程：先根据GN错误检查工程路径和文件名，再根据编译器错误检查函数声明顺序，最后通过烧录和串口数据验证修改结果。相比只得到一组温湿度数值，这一过程让我对Linux文件名规则、C语言声明与定义的关系以及OpenHarmony线程间同步机制都有了更加具体的理解。
